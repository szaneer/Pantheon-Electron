# Production environment variables
# This token should have minimal permissions (read-only access to releases)
# Create at: https://github.com/settings/tokens
# Scopes needed: repo (read-only) for private repos
GH_TOKEN_UPDATES=your_read_only_token_here